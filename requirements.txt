# Deliberately minimal — no UI dependencies until there is a UI, and no
# pinned versions yet while the shape of the project moves.

requests        # SEC EDGAR, and the constituent-list fetch
pandas          # tabular work throughout
lxml            # HTML table parsing for the constituent list
yfinance        # Stage 2 prices. Unofficial Yahoo client — trusted for
                # prices only, never for fundamentals (those come from EDGAR).
